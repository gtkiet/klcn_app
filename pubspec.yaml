name: klcn_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # HTTP client
  http: ^1.2.1

  # Local storage (bỏ comment khi tích hợp)
  # shared_preferences: ^2.3.2
  # flutter_secure_storage: ^9.2.2

  # Image picker (upload avatar)
  # image_picker: ^1.1.2

  # Navigation
  # go_router: ^14.2.7

  # Deep link (VNPay / MoMo callback)
  # app_links: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
  #   - assets/icons/